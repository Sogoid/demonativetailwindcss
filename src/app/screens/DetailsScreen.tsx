import * as React from 'react';
import {Text, View} from 'react-native';

const DetailsScreen = () => {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-lg text-blue-500">Details Screen</Text>
    </View>
  );
};

export default DetailsScreen;
